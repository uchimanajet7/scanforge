/**
 * 検出オーバーレイ描画設定。
 */
export const OVERLAY_CONFIG = Object.freeze({
  COLORS: ['#0ea5e9', '#f97316', '#22c55e', '#a855f7', '#eab308', '#ef4444'],
  FADE_DURATION_MS: 400,
  FADE_AFTER_MS: 1000,
  STROKE_WIDTH: 3,
  LABEL_FONT: '600 16px system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  LABEL_PADDING_X: 8,
  LABEL_HEIGHT: 24,
});
