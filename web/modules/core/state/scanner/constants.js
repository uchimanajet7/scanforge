/**
 * スキャナーサブストア共通の定数。
 */

export const IMAGE_ACTIVE_STATUSES = Object.freeze(['queued', 'preparing', 'scanning', 'retrying']);
export const IMAGE_TERMINAL_STATUSES = Object.freeze(['success', 'failed', 'canceled']);

