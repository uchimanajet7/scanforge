#!/usr/bin/env bash
set -euo pipefail

# ============================================================================
# 開発サーバー起動ラッパー
# キャッシュバスティングを適用してからSWSを起動する
# ============================================================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 引数を渡してキャッシュバスティングを適用
"${SCRIPT_DIR}/apply-cache-bust.sh" "$@"

# SWSを起動
exec "${SCRIPT_DIR}/run-sws.sh"
